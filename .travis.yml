language: cpp
os:
  - linux
compiler:
  - clang

install:
- sudo add-apt-repository -y ppa:bpaquet/zeromq4-precise
- sudo apt-get install libzmq-dev
- cmake -H. -Bcmake-out -G "Unix Makefiles"
- make -C cmake-out